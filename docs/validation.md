# 验证与限制

## 0.3.6 — 2026-09-13

- 源码完整回归：`python -X utf8 -B -m unittest discover -q`，215 项通过。
- Windows x64 便携包由构建器生成，ZIP 包含 1,100 个文件，大小 75,676,256 字节，SHA-256 为 `4820b2857dab138d3942215d7caf9adc2f5709c326dd3ba2fee61563c0279195`。
- 重新打开 ZIP 校验归档完整性、离线 UE4SS 运行库、私人运行时数据排除，以及 `lock_library`、`overlay_lock_library`、`native_support`、`overlay_native` 与 `startup_window` 已进入冻结程序。
- 此构建终端为普通权限，而发行 EXE 要求管理员权限；因此未在该终端执行冻结 EXE 的 `--self-test`。首次运行需接受 Windows 的管理员提示，以便读取同样以管理员身份运行的游戏。
- 主动原生功能仍默认关闭；构建清单保持 `native_game_verified: false`，不以源码或打包检查替代每台电脑上的游戏内验证。

## 0.3.5 — 2026-09-10（历史记录）

检查日期：2026-09-10。对象为 0.3.5 源码与本次 Windows x64 便携包。

## 已检查

- Windows 构建环境运行 `python -X utf8 -B -m unittest discover -q`：204 项测试通过，65.243 秒，退出码 0。
- 65 个 Python 文件编译检查、5 个 JSON 解析检查与 Markdown 本地链接检查通过；暂存差异通过 `git diff --cached --check`。
- 覆盖设置保存、三栏目开关、自动锁定与回收联动、窗口与页面生命周期、游戏路径发现、更新检查、原生策略、自动开门条件、八卦入口投影及过期隐藏。
- 冻结 EXE 的 `--self-test` 成功，报告版本 0.3.5、`frozen: true`；读取到 Tk 与运行目录资源。
- 冻结 EXE 在独立测试数据目录中连接本机正在运行的游戏，`--smoke-test` 返回 `success: true`、状态 `watching`，取得当前背包。该过程只读，不发送原生动作请求。
- EXE 字符串与数值版本资源均为 0.3.5。构建器检查包内模块、Lua 文件、离线运行库散列、私人数据排除及 ZIP 完整性。
- README 截图来自隔离演示配置下的实际程序，不是概念渲染图。
- GitHub v0.3.5 以预览版发布，发布附件的服务端 SHA-256 与本地一致；重新下载完整 ZIP 后复核一致。散列为 `71aa58a097fd584ae32fa6d5b162eec902f0e7120e2400fb743f9288ba3f16bf`，大小 75,654,637 字节。
- 两张 GitHub 截图的 Git 对象散列与本地文件一致。

## 尚有限制

- 自动开门与八卦入口提示的独立原型已有本机试用记录；合并后的 0.3.5 游戏组件仍需重启后核对。模拟游戏对象与只读背包检查不能证明所有原生动作都已在当前游戏内成功。
- 因此构建清单保持 `native_game_verified: false`，主动功能默认关闭。
- 部分 Tk 测试退出时仍有 `ThemeChanged` / 已销毁应用的回调提示；该轮测试结果为 OK，不据此声称所有运行期界面问题均已消失。
- 真正的独占全屏可能覆盖普通桌面悬浮层，可选择无边框全屏。
- GitHub 无法连接时只能提示检测失败并提供蓝奏云入口，不能据此判断已是最新版本。蓝奏云文件是否完成上传，以文件夹内实际版本和本次发行说明为准。

公开包不包含个人背包快照、存档或一次性装备修改程序。自检产生的本地报告不上传到仓库。最终 ZIP 的 SHA-256 见对应 Release 中的 `SHA256SUMS.txt`，发布与下载校验分别记录，不把本地构建称为已上传。

回归命令与构建方法见[开发说明](development.md)。
