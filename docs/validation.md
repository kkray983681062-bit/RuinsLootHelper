# 验证与限制

检查日期：2026-09-09。对象是本仓库本次公开的源码副本，不是旧版 ZIP 或正在运行的游戏。

## 已检查

- 在 Windows 本地使用项目构建环境执行 `python -X utf8 -B -m unittest discover -q`：141 项测试通过，耗时 96.019 秒，退出码 0。
- 测试覆盖安装发现、当前背包筛选、设置保存、Tk 界面、原生策略与模拟游戏对象等。
- 50 个 Python 文件编译检查通过；5 个运行或链接 JSON 可正常解析。
- 应用本地模块依赖齐全；公开目录未发现个人用户目录、Steam 账号记录、密钥形态字符串或游戏二进制资源。
- 蓝奏云文件夹及密码已在浏览器验证，文件夹当前为空。

## 尚有限制

- 测试退出时出现过 Tk `ThemeChanged` 销毁提示；该轮测试结果为 OK。尚未据此宣称运行期界面问题全部解决。
- 本次未重新构建或上传 EXE。打包过滤已允许正常的拾取库，但本次没有执行完整二进制构建。
- 原生拾取、锁定、回收、新掉落屏蔽等不能用本地测试代替当前游戏版本实测；兼容范围以对应发行说明为准。
- 当前源码界面仍显示 0.3.0。它与同号旧 ZIP 不是同一份功能快照；首次正式分发前应更新版本号并绑定实际构建产物。

当前回归命令与构建方法见[开发说明](development.md)。
